## Volatile
# Volatile is used on variables, there is no locking
# Volatile guranteed visibility of variables across different threads, but no atomicity is guranteed
# Volatile is most used for flag variables as the operations on these variables are atomatic.
